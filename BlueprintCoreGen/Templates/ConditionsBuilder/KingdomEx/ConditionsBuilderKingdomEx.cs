namespace BlueprintCoreGen.Conditions.Builder.KingdomEx
{
  /// <summary>
  /// Extension to <see cref="ConditionsBuilder"/> for conditions involving the Kingdom and Crusade system.
  /// </summary>
  /// <inheritdoc cref="ConditionsBuilder"/>
  public static class ConditionsBuilderKingdomEx
  {
    //----- Auto Generated -----//

    // [Generate(Kingmaker.UnitLogic.Mechanics.Conditions.HasTacticalMorale)]
    // [Generate(Kingmaker.UnitLogic.Mechanics.Conditions.TacticalCombatSquadHitPointsCondition)]
    // [Generate(Kingmaker.UnitLogic.Mechanics.Conditions.TargetHasArmyTag)]
    // [Generate(Kingmaker.UnitLogic.Mechanics.Conditions.ContextConditionGarrisonClear)]
    // [Generate(Kingmaker.Kingdom.Flags.AnySettlementUnderSiege)]
    // [Generate(Kingmaker.Kingdom.Flags.KingdomMoraleFlagCondition)]
    // [Generate(Kingmaker.Kingdom.Conditions.ArmyInLocationDefeated)]
    // [Generate(Kingmaker.Kingdom.Conditions.AutoKingdom)]
    // [Generate(Kingmaker.Kingdom.Conditions.BuildingHasNeighbours)]
    // [Generate(Kingmaker.Kingdom.Conditions.DaysTillNextMonth)]
    // [Generate(Kingmaker.Kingdom.Conditions.EventLifetime)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomAlignmentIs)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomAllArmiesInRegionDefeated)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomArtisanState)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomBuffIsActive)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomChapterWeek)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomDay)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomEventCanStart)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomEventIsActive)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomEventIsBeingResolved)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomExists)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomHasResolvableEvent)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomHasUpgradeableSettlement)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomIsVisible)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomLeaderIs)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomProjectIsAvailable)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomProjectIsDone)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomRankUpConditions)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomRegionIsConquered)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomRegionIsUpgraded)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomSettlementCount)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomSettlementHasBuilding)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomStatCheck)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomStatIsMaximum)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomTaskResolvedBy)]
    // [Generate(Kingmaker.Kingdom.Conditions.KingdomUnrestCheck)]

  }
}